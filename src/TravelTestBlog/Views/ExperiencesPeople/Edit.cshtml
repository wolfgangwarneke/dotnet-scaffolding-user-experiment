@model TravelTestBlog.Models.ExperiencePerson

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>ExperiencePerson</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="ExperiencePersonId" />
        <div class="form-group">
            <label asp-for="ExperienceId" class="control-label col-md-2"></label>
            <div class="col-md-10">
                <select asp-for="ExperienceId" class="form-control" asp-items="ViewBag.ExperienceId"></select>
                <span asp-validation-for="ExperienceId" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <label asp-for="PersonId" class="control-label col-md-2"></label>
            <div class="col-md-10">
                <select asp-for="PersonId" class="form-control" asp-items="ViewBag.PersonId"></select>
                <span asp-validation-for="PersonId" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
